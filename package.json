{
  "name": "pipeline-basico",
  "version": "1.0.0",
  "description": "Práctica: pipeline CI básico con GitHub Actions",
  "main": "src/index.js",
  "scripts": {
    "build": "node --check src/index.js",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
